# Install with:
#   pip install -r requirements.txt --break-system-packages
#
# picamera2 is NOT listed here — install it via apt (it ships prebuilt
# libcamera bindings that pip cannot provide):
#   sudo apt install -y python3-picamera2

Flask==3.1.1
Werkzeug==3.1.3
waitress==3.0.2
requests==2.32.3
Pillow==11.1.0
boto3==1.42.21
pigpio==1.78
Adafruit-Blinka==9.1.0
adafruit-circuitpython-pca9685==3.4.22
